pipeline {
    agent any
    stages {
        stage ('Test') {
            sh 'mvn -f my-app/pom.xml test'
        }
        stage('Build') { 
            steps {
                sh ' mvn -f my-app/pom.xml -B -DskipTests clean package' 
            }
        }
    }
}